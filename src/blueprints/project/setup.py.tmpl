from setuptools import find_packages, setup


setup(
    name="[[ name ]]",
    version="1.0",
    python_requires=">=3.6,<4.0",
    install_requires=[
        # "proper = ~=1.190505",   # Awesome web framework
        "argon2-cffi ~= 19.2.0",  # Next-gen hash function for the passwords
        "jinja2 ~= 2.10",         # Template system (http://jinja.pocoo.org/)
        "pony ~= 0.7.10",         # The smartest Python ORM
        "psycopg2 ~= 2.7",        # PostgreSQL driver
        "pyceo ~= 2.191002",      # Used to build the command-line interface
        "proper-form",            # Form-handling library that doesn't suck
        "redis ~= 3.2",           # Redis driver
        "whitenoise ~= 4.0",      # Serve static files, even in production!
    ],
    packages=find_packages(),
)
