{#def form, action='', method='post' #}

{% set f = form.fields %}

<Form
  method={method}
  action={action}
>
  [% for f in form_fields -%]
  <div class="my-5">
    <label>
      [[ f.name|title ]]
      <input
        type="text"
        class="block form-input"
        name="{{ f.[[ f.name ]].name }}"
        value="{{ f.[[ f.name ]].value }}"
      />
    </label>
    {% if f.[[ f.name ]].error -%}
      <div class="text-red-500">{{ f.[[ f.name ]].error.msg }}</div>
    {%- endif %}
  </div>
  [% endfor -%]

  {{ content }}
</Form>
