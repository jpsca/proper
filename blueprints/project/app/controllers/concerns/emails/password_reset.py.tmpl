from [[ name ]].app import app


def send_password_reset_email(user):
    token = user.auth.get_timestamped_token()
    url = app.url_for("Sessions.reset_validate", token=token, _external=True)
    print(url)  # TODO
