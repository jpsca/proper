const colors = require("tailwindcss/colors");

module.exports = {
	purge: [
		"../[[ name ]]/templates/**/*.html.jinja"
	],
	darkMode: false, // `false`, `"media"` or `"class"`
	theme: {},
	variants: {
		extend: {},
	},
	plugins: []
};
