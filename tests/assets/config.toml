# -------------------------------------------------------------
# Development config
# -------------------------------------------------------------

hello = "world"
secret_key = "Not secure. It is a BAD idea not using encrypted secrets!"
