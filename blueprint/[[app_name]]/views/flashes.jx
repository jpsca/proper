{% set flashes = current.request.flashes %}

{%- if flashes %}
<div class="my-1 text-center" aria-live="polite">
	{% for category, msg in flashes -%}
	<div class="alert alert--{{ category }}" role="alert">
		<p>{{ msg }}</p>
	</div>
	{% endfor -%}
</div>
{%- endif %}
